{% extends "app/index.html.twig" %}

{% block content %}
	{{ form_start(form) }}
		{{ form_errors(form) }}
		{{ success_message }}
		{{ form_row(form.oldPassword) }}
		{{ form_row(form.password) }}
		{{ form_row(form.submit) }}
	{{ form_end(form) }}
{% endblock %}